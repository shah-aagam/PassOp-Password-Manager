var CONFIG = {
    FRONTEND_URL: "https://pass-op-password-manager-aagam.vercel.app/",
    BACKEND_URL: "13.232.213.231:8000"
};

// Only log if 'console' exists (it usually does in both)
console.log("Config loaded");